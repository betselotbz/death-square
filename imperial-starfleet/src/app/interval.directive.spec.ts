import { IntervalDirective } from './interval.directive';

describe('IntervalDirective', () => {
  it('should create an instance', () => {
    const directive = new IntervalDirective();
    expect(directive).toBeTruthy();
  });
});
